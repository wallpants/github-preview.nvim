import { ThemePicker } from "./theme-picker";

export const Option = () => {
    return (
        <div className="rounded border border-github-border-default">
            <ThemePicker />
        </div>
    );
};
