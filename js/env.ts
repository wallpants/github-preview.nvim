export const ENV = {
    NVIM_LISTEN_ADDRESS: process.env["NVIM_LISTEN_ADDRESS"],
    VITE_GP_PORT: process.env["VITE_GP_PORT"],

    // lsp logs
    LSP_SERVER_STREAM: process.env["LSP_SERVER_STREAM"],
    LSP_LOG_LEVEL: process.env["LSP_LOG_LEVEL"],
};
